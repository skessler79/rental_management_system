package main.enums;

//this enum class is to standardize input for user types (with the REGULAR being normal user that is the potential tenant)
public enum UserType {
    ADMIN,
    OWNER,
    AGENT,
    REGULAR
}
